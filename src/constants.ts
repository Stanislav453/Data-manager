export const DEFAULT = 'users';

//FORMIK
export const required = 'Required';
export const onlyLetters = 'Only letters';
export const minLetterName = 4;
export const minLetterNameError = `Min ${minLetterName} characters`;
export  const minAge = 1;
export  const ageError = `Age cant be under ${minAge}`;
